const TM_STATE_deleteEndSymbol_state = state_description.states.indexOf('deleteEndSymbol_state');
const TM_STATE_stackInvConditionalAppend0_state = state_description.states.indexOf('stackInvConditionalAppend0_state');
const TM_STATE_and_scanLeft = state_description.states.indexOf('and_scanLeft');
const TM_STATE_uop_decode_101110 = state_description.states.indexOf('uop_decode_101110');
const TM_STATE_and_readVal = state_description.states.indexOf('and_readVal');
const TM_STATE_nextDollarLeft_state = state_description.states.indexOf('nextDollarLeft_state');
const TM_STATE_copyToUstackEnd_state = state_description.states.indexOf('copyToUstackEnd_state');
const TM_STATE_and_read0_0 = state_description.states.indexOf('and_read0_0');
const TM_STATE_uop_decode_101111 = state_description.states.indexOf('uop_decode_101111');
const TM_STATE_stackConditionalAppendDollar_state = state_description.states.indexOf('stackConditionalAppendDollar_state');
const TM_STATE_addWithCarry_halfAdd1_1 = state_description.states.indexOf('addWithCarry_halfAdd1_1');
const TM_STATE_stackInvConditionalAppend1_state = state_description.states.indexOf('stackInvConditionalAppend1_state');
const TM_STATE_and_read1_0 = state_description.states.indexOf('and_read1_0');
const TM_STATE_uop_decode_1011110 = state_description.states.indexOf('uop_decode_1011110');
const TM_STATE_append0_state = state_description.states.indexOf('append0_state');
const TM_STATE_addWithCarry_halfAdd0_1 = state_description.states.indexOf('addWithCarry_halfAdd0_1');
const TM_STATE_and_read0_1 = state_description.states.indexOf('and_read0_1');
const TM_STATE_copyPopFromEnd_findNext = state_description.states.indexOf('copyPopFromEnd_findNext');
const TM_STATE_copyMsb_readBit = state_description.states.indexOf('copyMsb_readBit');
const TM_STATE_uop_decode_10111100 = state_description.states.indexOf('uop_decode_10111100');
const TM_STATE_addWithCarry_halfAdd2_0 = state_description.states.indexOf('addWithCarry_halfAdd2_0');
const TM_STATE_and_read1_1 = state_description.states.indexOf('and_read1_1');
const TM_STATE_copyPopFromEnd_copy1 = state_description.states.indexOf('copyPopFromEnd_copy1');
const TM_STATE_copyMsb_write0 = state_description.states.indexOf('copyMsb_write0');
const TM_STATE_stackConditionalAppend1_state = state_description.states.indexOf('stackConditionalAppend1_state');
const TM_STATE_addWithCarry_halfAdd1_0 = state_description.states.indexOf('addWithCarry_halfAdd1_0');
const TM_STATE_and_write0 = state_description.states.indexOf('and_write0');
const TM_STATE_copyPopFromEnd_copy0 = state_description.states.indexOf('copyPopFromEnd_copy0');
const TM_STATE_copyMsb_write1 = state_description.states.indexOf('copyMsb_write1');
const TM_STATE_stackInvConditionalAppendDollar_state = state_description.states.indexOf('stackInvConditionalAppendDollar_state');
const TM_STATE_addWithCarry_halfAdd0_0 = state_description.states.indexOf('addWithCarry_halfAdd0_0');
const TM_STATE_uop_decode_1011111 = state_description.states.indexOf('uop_decode_1011111');
const TM_STATE_and_write1 = state_description.states.indexOf('and_write1');
const TM_STATE_copyPopFromEnd_readSym = state_description.states.indexOf('copyPopFromEnd_readSym');
const TM_STATE_addWithCarry_carry1 = state_description.states.indexOf('addWithCarry_carry1');
const TM_STATE_uop_decode_10111110 = state_description.states.indexOf('uop_decode_10111110');
const TM_STATE_copyPopFromEnd_removeLastDollar = state_description.states.indexOf('copyPopFromEnd_removeLastDollar');
const TM_STATE_stackConditionalAppend0_state = state_description.states.indexOf('stackConditionalAppend0_state');
const TM_STATE_addWithCarry_carry0 = state_description.states.indexOf('addWithCarry_carry0');
const TM_STATE_uop_decode_101111100 = state_description.states.indexOf('uop_decode_101111100');
const TM_STATE_uop_decode_0 = state_description.states.indexOf('uop_decode_0');
const TM_STATE_copyPopFromEnd_findEnd = state_description.states.indexOf('copyPopFromEnd_findEnd');
const TM_STATE_doUInst_rewindUcodeHead = state_description.states.indexOf('doUInst_rewindUcodeHead');
const TM_STATE_addWithCarry_carry1Left1 = state_description.states.indexOf('addWithCarry_carry1Left1');
const TM_STATE_uop_decode_1011111000 = state_description.states.indexOf('uop_decode_1011111000');
const TM_STATE_copyPopFromEnd_clearWord = state_description.states.indexOf('copyPopFromEnd_clearWord');
const TM_STATE_stackAppendDollar_state = state_description.states.indexOf('stackAppendDollar_state');
const TM_STATE_doUInst_rewindStackHead = state_description.states.indexOf('doUInst_rewindStackHead');
const TM_STATE_addWithCarry_carry0Left1 = state_description.states.indexOf('addWithCarry_carry0Left1');
const TM_STATE_uop_decode_10111110000 = state_description.states.indexOf('uop_decode_10111110000');
const TM_STATE_doUInst_compareInst = state_description.states.indexOf('doUInst_compareInst');
const TM_STATE_uop_decode_1 = state_description.states.indexOf('uop_decode_1');
const TM_STATE_addWithCarry_carry1Left = state_description.states.indexOf('addWithCarry_carry1Left');
const TM_STATE_copyLsb_readBit = state_description.states.indexOf('copyLsb_readBit');
const TM_STATE_stackAppend1_state = state_description.states.indexOf('stackAppend1_state');
const TM_STATE_uop_decode_101111100001 = state_description.states.indexOf('uop_decode_101111100001');
const TM_STATE_doUInst_findNextInst0 = state_description.states.indexOf('doUInst_findNextInst0');
const TM_STATE_uop_decode_10 = state_description.states.indexOf('uop_decode_10');
const TM_STATE_addWithCarry_carry0Left = state_description.states.indexOf('addWithCarry_carry0Left');
const TM_STATE_copyLsb_write0 = state_description.states.indexOf('copyLsb_write0');
const TM_STATE_doUInst_findNextInst1 = state_description.states.indexOf('doUInst_findNextInst1');
const TM_STATE_stackAppend0_state = state_description.states.indexOf('stackAppend0_state');
const TM_STATE_addWithCarry_readCarry = state_description.states.indexOf('addWithCarry_readCarry');
const TM_STATE_copyLsb_write1 = state_description.states.indexOf('copyLsb_write1');
const TM_STATE_uop_decode_101 = state_description.states.indexOf('uop_decode_101');
const TM_STATE_doUInst_popStack = state_description.states.indexOf('doUInst_popStack');
const TM_STATE_uop_decode_1010 = state_description.states.indexOf('uop_decode_1010');
const TM_STATE_doUInst_illegalInst = state_description.states.indexOf('doUInst_illegalInst');
const TM_STATE_uop_root = state_description.states.indexOf('uop_root');
const TM_STATE_uop_decode_10100 = state_description.states.indexOf('uop_decode_10100');
const TM_STATE_uop_decode_101000 = state_description.states.indexOf('uop_decode_101000');
const TM_STATE_or_scanLeft = state_description.states.indexOf('or_scanLeft');
const TM_STATE_or_readVal = state_description.states.indexOf('or_readVal');
const TM_STATE_writeToMem_rewind = state_description.states.indexOf('writeToMem_rewind');
const TM_STATE_copyToEnd_copyDollar = state_description.states.indexOf('copyToEnd_copyDollar');
const TM_STATE_or_read0_0 = state_description.states.indexOf('or_read0_0');
const TM_STATE_writeToMem_copy = state_description.states.indexOf('writeToMem_copy');
const TM_STATE_uop_decode_101001 = state_description.states.indexOf('uop_decode_101001');
const TM_STATE_copyToEnd_reverse1 = state_description.states.indexOf('copyToEnd_reverse1');
const TM_STATE_or_read1_0 = state_description.states.indexOf('or_read1_0');
const TM_STATE_uop_decode_1010010 = state_description.states.indexOf('uop_decode_1010010');
const TM_STATE_copyToEnd_reverse0 = state_description.states.indexOf('copyToEnd_reverse0');
const TM_STATE_or_read0_1 = state_description.states.indexOf('or_read0_1');
const TM_STATE_uop_decode_10100101 = state_description.states.indexOf('uop_decode_10100101');
const TM_STATE_copyToEnd_copy1 = state_description.states.indexOf('copyToEnd_copy1');
const TM_STATE_or_read1_1 = state_description.states.indexOf('or_read1_1');
const TM_STATE_copyToEnd_copy0 = state_description.states.indexOf('copyToEnd_copy0');
const TM_STATE_or_write0 = state_description.states.indexOf('or_write0');
const TM_STATE_copyToEnd_readSym = state_description.states.indexOf('copyToEnd_readSym');
const TM_STATE_or_write1 = state_description.states.indexOf('or_write1');
const TM_STATE_uop_decode_10101 = state_description.states.indexOf('uop_decode_10101');
const TM_STATE_uop_decode_1011 = state_description.states.indexOf('uop_decode_1011');
const TM_STATE_overwriteWithMemLoad_rewind = state_description.states.indexOf('overwriteWithMemLoad_rewind');
const TM_STATE_uop_decode_10110 = state_description.states.indexOf('uop_decode_10110');
const TM_STATE_overwriteWithMemLoad_copy = state_description.states.indexOf('overwriteWithMemLoad_copy');
const TM_STATE_addWithCarry_writeDollar = state_description.states.indexOf('addWithCarry_writeDollar');
const TM_STATE_uop_decode_101100 = state_description.states.indexOf('uop_decode_101100');
const TM_STATE_addWithCarry_carry1Right1 = state_description.states.indexOf('addWithCarry_carry1Right1');
const TM_STATE_uop_decode_1011000 = state_description.states.indexOf('uop_decode_1011000');
const TM_STATE_deleteMiddleSymbol_gotoEnd = state_description.states.indexOf('deleteMiddleSymbol_gotoEnd');
const TM_STATE_addWithCarry_carry1Right0 = state_description.states.indexOf('addWithCarry_carry1Right0');
const TM_STATE_deleteMiddleSymbol_writeDollar = state_description.states.indexOf('deleteMiddleSymbol_writeDollar');
const TM_STATE_addWithCarry_carry0Right1 = state_description.states.indexOf('addWithCarry_carry0Right1');
const TM_STATE_uop_decode_10110001 = state_description.states.indexOf('uop_decode_10110001');
const TM_STATE_deleteMiddleSymbol_write1 = state_description.states.indexOf('deleteMiddleSymbol_write1');
const TM_STATE_addWithCarry_carry0Right0 = state_description.states.indexOf('addWithCarry_carry0Right0');
const TM_STATE_uop_decode_101100010 = state_description.states.indexOf('uop_decode_101100010');
const TM_STATE_memHeadRight_rewind = state_description.states.indexOf('memHeadRight_rewind');
const TM_STATE_deleteMiddleSymbol_write0 = state_description.states.indexOf('deleteMiddleSymbol_write0');
const TM_STATE_addWithCarry_fullAdd3 = state_description.states.indexOf('addWithCarry_fullAdd3');
const TM_STATE_uop_decode_1011000100 = state_description.states.indexOf('uop_decode_1011000100');
const TM_STATE_memHeadRight_skipZeros = state_description.states.indexOf('memHeadRight_skipZeros');
const TM_STATE_gotoBeginning_noDollar = state_description.states.indexOf('gotoBeginning_noDollar');
const TM_STATE_deleteMiddleSymbol_readSymbol = state_description.states.indexOf('deleteMiddleSymbol_readSymbol');
const TM_STATE_addWithCarry_fullAdd2 = state_description.states.indexOf('addWithCarry_fullAdd2');
const TM_STATE_not_state = state_description.states.indexOf('not_state');
const TM_STATE_uop_decode_10110001000 = state_description.states.indexOf('uop_decode_10110001000');
const TM_STATE_deleteMiddleSymbol_inBlank = state_description.states.indexOf('deleteMiddleSymbol_inBlank');
const TM_STATE_gotoBeginning_foundDollar = state_description.states.indexOf('gotoBeginning_foundDollar');
const TM_STATE_addWithCarry_fullAdd1 = state_description.states.indexOf('addWithCarry_fullAdd1');
const TM_STATE_uop_decode_101100010000 = state_description.states.indexOf('uop_decode_101100010000');
const TM_STATE_addWithCarry_fullAdd0 = state_description.states.indexOf('addWithCarry_fullAdd0');
const TM_STATE_memHeadLeft_skipOnes = state_description.states.indexOf('memHeadLeft_skipOnes');
const TM_STATE_addWithCarry_halfAdd2_1 = state_description.states.indexOf('addWithCarry_halfAdd2_1');
const TM_STATE_appendDollar_state = state_description.states.indexOf('appendDollar_state');
const TM_STATE_memHeadLeft_rewind = state_description.states.indexOf('memHeadLeft_rewind');
const TM_STATE_gotoEnd_state = state_description.states.indexOf('gotoEnd_state');
const TM_STATE_iszero_scan = state_description.states.indexOf('iszero_scan');
const TM_STATE_iszero_write0 = state_description.states.indexOf('iszero_write0');
const TM_STATE_uop_decode_1011001 = state_description.states.indexOf('uop_decode_1011001');
const TM_STATE_popFromEnd_state = state_description.states.indexOf('popFromEnd_state');
const TM_STATE_append1_state = state_description.states.indexOf('append1_state');
const TM_STATE_iszero_write1 = state_description.states.indexOf('iszero_write1');
const TM_STATE_nextDollarRight_state = state_description.states.indexOf('nextDollarRight_state');
const TM_STATE_uop_decode_10111 = state_description.states.indexOf('uop_decode_10111');

const tm_step = () => {
    switch (curr_state) {
        case TM_STATE_deleteEndSymbol_state:
            if (1) {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_stackInvConditionalAppend0_state:
            if (reg_tape[reg_tape_head]=='0') {
                stack_tape[stack_tape_head]='0';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_and_scanLeft:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_readVal;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_scanLeft;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101110:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_readSym;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_appendDollar_state;
                return 0;
            }
            return 1;
        case TM_STATE_and_readVal:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_and_read0_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_and_read1_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_nextDollarLeft_state:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_nextDollarLeft_state;
                return 0;
            }
            return 1;
        case TM_STATE_copyToUstackEnd_state:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                stack_tape[stack_tape_head]='0';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_copyToUstackEnd_state;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                stack_tape[stack_tape_head]='1';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_copyToUstackEnd_state;
                return 0;
            }
            return 1;
        case TM_STATE_and_read0_0:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_read0_1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_and_read0_0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101111:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1011110;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1011111;
                return 0;
            }
            return 1;
        case TM_STATE_stackConditionalAppendDollar_state:
            if (reg_tape[reg_tape_head]=='1') {
                stack_tape[stack_tape_head]='$';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_halfAdd1_1:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd1;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd2;
                return 0;
            }
            return 1;
        case TM_STATE_stackInvConditionalAppend1_state:
            if (reg_tape[reg_tape_head]=='0') {
                stack_tape[stack_tape_head]='1';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_and_read1_0:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_read1_1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_and_read1_0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1011110:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10111100;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_stackInvConditionalAppend1_state;
                return 0;
            }
            return 1;
        case TM_STATE_append0_state:
            if (1) {
                reg_tape[reg_tape_head]='0';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_halfAdd0_1:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd1;
                return 0;
            }
            return 1;
        case TM_STATE_and_read0_1:
            if (1) {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_write0;
                return 0;
            }
            return 1;
        case TM_STATE_copyPopFromEnd_findNext:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyPopFromEnd_readSym;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyPopFromEnd_findNext;
                return 0;
            }
            return 1;
        case TM_STATE_copyMsb_readBit:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape_head++;
                curr_state=TM_STATE_copyMsb_write0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape_head++;
                curr_state=TM_STATE_copyMsb_write1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10111100:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_stackConditionalAppendDollar_state;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_not_state;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_halfAdd2_0:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_halfAdd2_1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_halfAdd2_0;
                return 0;
            }
            return 1;
        case TM_STATE_and_read1_1:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_write0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_write1;
                return 0;
            }
            return 1;
        case TM_STATE_copyPopFromEnd_copy1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                reg_tape_head++;
                curr_state=TM_STATE_copyPopFromEnd_findNext;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyPopFromEnd_copy1;
                return 0;
            }
            return 1;
        case TM_STATE_copyMsb_write0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyMsb_write0;
                return 0;
            }
            return 1;
        case TM_STATE_stackConditionalAppend1_state:
            if (reg_tape[reg_tape_head]=='1') {
                stack_tape[stack_tape_head]='1';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_halfAdd1_0:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_halfAdd1_1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_halfAdd1_0;
                return 0;
            }
            return 1;
        case TM_STATE_and_write0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_readVal;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_write0;
                return 0;
            }
            return 1;
        case TM_STATE_copyPopFromEnd_copy0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                reg_tape_head++;
                curr_state=TM_STATE_copyPopFromEnd_findNext;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyPopFromEnd_copy0;
                return 0;
            }
            return 1;
        case TM_STATE_copyMsb_write1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyMsb_write1;
                return 0;
            }
            return 1;
        case TM_STATE_stackInvConditionalAppendDollar_state:
            if (reg_tape[reg_tape_head]=='0') {
                stack_tape[stack_tape_head]='$';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_halfAdd0_0:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_halfAdd0_1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_halfAdd0_0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1011111:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10111110;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_stackConditionalAppend1_state;
                return 0;
            }
            return 1;
        case TM_STATE_and_write1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_readVal;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_write1;
                return 0;
            }
            return 1;
        case TM_STATE_copyPopFromEnd_readSym:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyPopFromEnd_copy0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyPopFromEnd_copy1;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry1:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_halfAdd1_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_halfAdd2_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_carry1Right0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10111110:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101111100;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_readCarry;
                return 0;
            }
            return 1;
        case TM_STATE_copyPopFromEnd_removeLastDollar:
            if (1) {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyPopFromEnd_readSym;
                return 0;
            }
            return 1;
        case TM_STATE_stackConditionalAppend0_state:
            if (reg_tape[reg_tape_head]=='1') {
                stack_tape[stack_tape_head]='0';
                reg_tape_head++;
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry0:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_halfAdd0_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_halfAdd1_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_carry0Right0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101111100:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1011111000;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_and_scanLeft;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_0:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_stackAppendDollar_state;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_stackAppend1_state;
                return 0;
            }
            return 1;
        case TM_STATE_copyPopFromEnd_findEnd:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyPopFromEnd_removeLastDollar;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyPopFromEnd_findEnd;
                return 0;
            }
            return 1;
        case TM_STATE_doUInst_rewindUcodeHead:
            if (ucode_tape[ucode_tape_head]=='_') {
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_rewindStackHead;
                return 0;
            }
            else {
                if (ucode_tape_head>0) {
                    ucode_tape_head--;
                }
                curr_state=TM_STATE_doUInst_rewindUcodeHead;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry1Left1:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry1;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry1Left1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1011111000:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10111110000;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_scanLeft;
                return 0;
            }
            return 1;
        case TM_STATE_copyPopFromEnd_clearWord:
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape_head++;
                curr_state=TM_STATE_copyPopFromEnd_findEnd;
                return 0;
            }
            else {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_copyPopFromEnd_clearWord;
                return 0;
            }
            return 1;
        case TM_STATE_stackAppendDollar_state:
            if (1) {
                stack_tape[stack_tape_head]='$';
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_doUInst_rewindStackHead:
            if (stack_tape[stack_tape_head]=='$') {
                stack_tape_head++;
                curr_state=TM_STATE_doUInst_compareInst;
                return 0;
            }
            else {
                if (stack_tape_head>0) {
                    stack_tape_head--;
                }
                curr_state=TM_STATE_doUInst_rewindStackHead;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry0Left1:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry0;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry0Left1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10111110000:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_writeToMem_copy;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101111100001;
                return 0;
            }
            return 1;
        case TM_STATE_doUInst_compareInst:
            if ((ucode_tape[ucode_tape_head]=='$')&&(stack_tape[stack_tape_head]=='_')) {
                if (stack_tape_head>0) {
                    stack_tape_head--;
                }
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_popStack;
                return 0;
            }
            if (((ucode_tape[ucode_tape_head]=='0')&&(stack_tape[stack_tape_head]=='0'))||((ucode_tape[ucode_tape_head]=='1')&&(stack_tape[stack_tape_head]=='1'))) {
                stack_tape_head++;
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_compareInst;
                return 0;
            }
            if ((stack_tape[stack_tape_head]=='_')&&(!(ucode_tape[ucode_tape_head]=='$'))) {
                if (stack_tape_head>0) {
                    stack_tape_head--;
                }
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_findNextInst0;
                return 0;
            }
            if ((!(stack_tape[stack_tape_head]=='_'))&&(ucode_tape[ucode_tape_head]=='$')) {
                if (stack_tape_head>0) {
                    stack_tape_head--;
                }
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_findNextInst1;
                return 0;
            }
            if ((ucode_tape[ucode_tape_head]=='_')&&(!(stack_tape[stack_tape_head]=='_'))) {
                curr_state=TM_STATE_doUInst_illegalInst;
                return 0;
            }
            else {
                if (stack_tape_head>0) {
                    stack_tape_head--;
                }
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_findNextInst0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_stackAppend0_state;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry1Left:
            if (1) {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry1Left1;
                return 0;
            }
            return 1;
        case TM_STATE_copyLsb_readBit:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape_head++;
                curr_state=TM_STATE_copyLsb_write0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape_head++;
                curr_state=TM_STATE_copyLsb_write1;
                return 0;
            }
            return 1;
        case TM_STATE_stackAppend1_state:
            if (1) {
                stack_tape[stack_tape_head]='1';
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101111100001:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_memHeadRight_skipZeros;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_overwriteWithMemLoad_copy;
                return 0;
            }
            return 1;
        case TM_STATE_doUInst_findNextInst0:
            if (ucode_tape[ucode_tape_head]=='$') {
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_findNextInst1;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='_') {
                curr_state=TM_STATE_doUInst_illegalInst;
                return 0;
            }
            else {
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_findNextInst0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_nextDollarRight_state;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry0Left:
            if (1) {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry0Left1;
                return 0;
            }
            return 1;
        case TM_STATE_copyLsb_write0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyLsb_write0;
                return 0;
            }
            return 1;
        case TM_STATE_doUInst_findNextInst1:
            if (ucode_tape[ucode_tape_head]=='$') {
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_rewindStackHead;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='_') {
                curr_state=TM_STATE_doUInst_illegalInst;
                return 0;
            }
            else {
                ucode_tape_head++;
                curr_state=TM_STATE_doUInst_findNextInst1;
                return 0;
            }
            return 1;
        case TM_STATE_stackAppend0_state:
            if (1) {
                stack_tape[stack_tape_head]='0';
                stack_tape_head++;
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_readCarry:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry0Left;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry1Left;
                return 0;
            }
            return 1;
        case TM_STATE_copyLsb_write1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyLsb_write1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1010;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1011;
                return 0;
            }
            return 1;
        case TM_STATE_doUInst_popStack:
            if (stack_tape[stack_tape_head]=='$') {
                stack_tape[stack_tape_head]='_';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                stack_tape[stack_tape_head]='_';
                if (stack_tape_head>0) {
                    stack_tape_head--;
                }
                curr_state=TM_STATE_doUInst_popStack;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1010:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10100;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10101;
                return 0;
            }
            return 1;
        case TM_STATE_doUInst_illegalInst:
            return 1;
        case TM_STATE_uop_root:
            if (ucode_tape[ucode_tape_head]=='$') {
                if (ucode_tape_head>0) {
                    ucode_tape_head--;
                }
                curr_state=TM_STATE_doUInst_rewindUcodeHead;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_0;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10100:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101000;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101001;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101000:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_gotoEnd_state;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_deleteEndSymbol_state;
                return 0;
            }
            return 1;
        case TM_STATE_or_scanLeft:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_readVal;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_scanLeft;
                return 0;
            }
            return 1;
        case TM_STATE_or_readVal:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_or_read0_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_or_read1_0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_writeToMem_rewind:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                if (mem_tape_head>0) {
                    mem_tape_head--;
                }
                curr_state=TM_STATE_writeToMem_rewind;
                return 0;
            }
            return 1;
        case TM_STATE_copyToEnd_copyDollar:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='$';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_copyDollar;
                return 0;
            }
            return 1;
        case TM_STATE_or_read0_0:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_read0_1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_or_read0_0;
                return 0;
            }
            return 1;
        case TM_STATE_writeToMem_copy:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_writeToMem_rewind;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                mem_tape[mem_tape_head]='0';
                reg_tape_head++;
                mem_tape_head++;
                curr_state=TM_STATE_writeToMem_copy;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                mem_tape[mem_tape_head]='1';
                reg_tape_head++;
                mem_tape_head++;
                curr_state=TM_STATE_writeToMem_copy;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101001:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1010010;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_copyPopFromEnd_clearWord;
                return 0;
            }
            return 1;
        case TM_STATE_copyToEnd_reverse1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_readSym;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyToEnd_reverse1;
                return 0;
            }
            return 1;
        case TM_STATE_or_read1_0:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_read1_1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_or_read1_0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1010010:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_copyMsb_readBit;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10100101;
                return 0;
            }
            return 1;
        case TM_STATE_copyToEnd_reverse0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_readSym;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyToEnd_reverse0;
                return 0;
            }
            return 1;
        case TM_STATE_or_read0_1:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_write0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_write1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10100101:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                reg_tape[reg_tape_head]='_';
                curr_state=TM_STATE_deleteMiddleSymbol_inBlank;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_append1_state;
                return 0;
            }
            return 1;
        case TM_STATE_copyToEnd_copy1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyToEnd_reverse1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_copy1;
                return 0;
            }
            return 1;
        case TM_STATE_or_read1_1:
            if (1) {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_write1;
                return 0;
            }
            return 1;
        case TM_STATE_copyToEnd_copy0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyToEnd_reverse0;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_copy0;
                return 0;
            }
            return 1;
        case TM_STATE_or_write0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_readVal;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_write0;
                return 0;
            }
            return 1;
        case TM_STATE_copyToEnd_readSym:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_copy0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_copy1;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape_head++;
                curr_state=TM_STATE_copyToEnd_copyDollar;
                return 0;
            }
            return 1;
        case TM_STATE_or_write1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_readVal;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_or_write1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10101:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_gotoBeginning_noDollar;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_append0_state;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1011:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10110;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10111;
                return 0;
            }
            return 1;
        case TM_STATE_overwriteWithMemLoad_rewind:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                if (mem_tape_head>0) {
                    mem_tape_head--;
                }
                curr_state=TM_STATE_overwriteWithMemLoad_rewind;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10110:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101100;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_nextDollarLeft_state;
                return 0;
            }
            return 1;
        case TM_STATE_overwriteWithMemLoad_copy:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_overwriteWithMemLoad_rewind;
                return 0;
            }
            if ((!(reg_tape[reg_tape_head]=='$'))&&(mem_tape[mem_tape_head]=='0')) {
                reg_tape[reg_tape_head]='0';
                reg_tape_head++;
                mem_tape_head++;
                curr_state=TM_STATE_overwriteWithMemLoad_copy;
                return 0;
            }
            if ((!(reg_tape[reg_tape_head]=='$'))&&(mem_tape[mem_tape_head]=='1')) {
                reg_tape[reg_tape_head]='1';
                reg_tape_head++;
                mem_tape_head++;
                curr_state=TM_STATE_overwriteWithMemLoad_copy;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_writeDollar:
            if (1) {
                reg_tape[reg_tape_head]='$';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101100:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1011000;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1011001;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry1Right1:
            if (1) {
                reg_tape[reg_tape_head]='1';
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_writeDollar;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1011000:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_stackInvConditionalAppend0_state;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10110001;
                return 0;
            }
            return 1;
        case TM_STATE_deleteMiddleSymbol_gotoEnd:
            if (1) {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry1Right0:
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_carry1Right1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_carry1Right0;
                return 0;
            }
            return 1;
        case TM_STATE_deleteMiddleSymbol_writeDollar:
            if (1) {
                reg_tape[reg_tape_head]='$';
                reg_tape_head++;
                curr_state=TM_STATE_deleteMiddleSymbol_inBlank;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry0Right1:
            if (1) {
                reg_tape[reg_tape_head]='0';
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_writeDollar;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10110001:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101100010;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_stackInvConditionalAppendDollar_state;
                return 0;
            }
            return 1;
        case TM_STATE_deleteMiddleSymbol_write1:
            if (1) {
                reg_tape[reg_tape_head]='1';
                reg_tape_head++;
                curr_state=TM_STATE_deleteMiddleSymbol_inBlank;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_carry0Right0:
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_carry0Right1;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_addWithCarry_carry0Right0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101100010:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_1011000100;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_copyLsb_readBit;
                return 0;
            }
            return 1;
        case TM_STATE_memHeadRight_rewind:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                mem_tape_head++;
                curr_state=TM_STATE_memHeadRight_skipZeros;
                return 0;
            }
            else {
                reg_tape[reg_tape_head]='1';
                reg_tape_head++;
                curr_state=TM_STATE_memHeadRight_rewind;
                return 0;
            }
            return 1;
        case TM_STATE_deleteMiddleSymbol_write0:
            if (1) {
                reg_tape[reg_tape_head]='0';
                reg_tape_head++;
                curr_state=TM_STATE_deleteMiddleSymbol_inBlank;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_fullAdd3:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry1;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd3;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1011000100:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_10110001000;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape_head++;
                curr_state=TM_STATE_iszero_scan;
                return 0;
            }
            return 1;
        case TM_STATE_memHeadRight_skipZeros:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_memHeadRight_skipZeros;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='0';
                reg_tape_head++;
                curr_state=TM_STATE_memHeadRight_rewind;
                return 0;
            }
            return 1;
        case TM_STATE_gotoBeginning_noDollar:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_gotoBeginning_foundDollar;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_gotoBeginning_noDollar;
                return 0;
            }
            return 1;
        case TM_STATE_deleteMiddleSymbol_readSymbol:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_deleteMiddleSymbol_write0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_deleteMiddleSymbol_write1;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_deleteMiddleSymbol_writeDollar;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_deleteMiddleSymbol_gotoEnd;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_fullAdd2:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry1;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd2;
                return 0;
            }
            return 1;
        case TM_STATE_not_state:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='1';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_not_state;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='0';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_not_state;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10110001000:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101100010000;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                if (mem_tape_head>0) {
                    mem_tape_head--;
                }
                curr_state=TM_STATE_memHeadLeft_skipOnes;
                return 0;
            }
            return 1;
        case TM_STATE_deleteMiddleSymbol_inBlank:
            if (1) {
                reg_tape_head++;
                curr_state=TM_STATE_deleteMiddleSymbol_readSymbol;
                return 0;
            }
            return 1;
        case TM_STATE_gotoBeginning_foundDollar:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_gotoBeginning_noDollar;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_fullAdd1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry0;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd1;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_101100010000:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                if (ucode_tape_head>0) {
                    ucode_tape_head--;
                }
                curr_state=TM_STATE_doUInst_rewindUcodeHead;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                reg_tape_head++;
                stack_tape[stack_tape_head]='$';
                stack_tape_head++;
                curr_state=TM_STATE_copyToUstackEnd_state;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_fullAdd0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_carry0;
                return 0;
            }
            else {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd0;
                return 0;
            }
            return 1;
        case TM_STATE_memHeadLeft_skipOnes:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_memHeadLeft_skipOnes;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='1';
                reg_tape_head++;
                curr_state=TM_STATE_memHeadLeft_rewind;
                return 0;
            }
            return 1;
        case TM_STATE_addWithCarry_halfAdd2_1:
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd2;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_addWithCarry_fullAdd3;
                return 0;
            }
            return 1;
        case TM_STATE_appendDollar_state:
            if (1) {
                reg_tape[reg_tape_head]='$';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_memHeadLeft_rewind:
            if (reg_tape[reg_tape_head]=='$') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                if (mem_tape_head>0) {
                    mem_tape_head--;
                }
                curr_state=TM_STATE_memHeadLeft_skipOnes;
                return 0;
            }
            else {
                reg_tape[reg_tape_head]='0';
                reg_tape_head++;
                curr_state=TM_STATE_memHeadLeft_rewind;
                return 0;
            }
            return 1;
        case TM_STATE_gotoEnd_state:
            if (reg_tape[reg_tape_head]=='_') {
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_gotoEnd_state;
                return 0;
            }
            return 1;
        case TM_STATE_iszero_scan:
            if (reg_tape[reg_tape_head]=='1') {
                reg_tape_head++;
                curr_state=TM_STATE_iszero_write0;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='$') {
                reg_tape_head++;
                curr_state=TM_STATE_iszero_write1;
                return 0;
            }
            if (reg_tape[reg_tape_head]=='0') {
                reg_tape_head++;
                curr_state=TM_STATE_iszero_scan;
                return 0;
            }
            return 1;
        case TM_STATE_iszero_write0:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='0';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_iszero_write0;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_1011001:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_popFromEnd_state;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_stackConditionalAppend0_state;
                return 0;
            }
            return 1;
        case TM_STATE_popFromEnd_state:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape[reg_tape_head]='_';
                if (reg_tape_head>0) {
                    reg_tape_head--;
                }
                curr_state=TM_STATE_popFromEnd_state;
                return 0;
            }
            return 1;
        case TM_STATE_append1_state:
            if (1) {
                reg_tape[reg_tape_head]='1';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            return 1;
        case TM_STATE_iszero_write1:
            if (reg_tape[reg_tape_head]=='_') {
                reg_tape[reg_tape_head]='1';
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_iszero_write1;
                return 0;
            }
            return 1;
        case TM_STATE_nextDollarRight_state:
            if (reg_tape[reg_tape_head]=='$') {
                curr_state=TM_STATE_uop_root;
                return 0;
            }
            else {
                reg_tape_head++;
                curr_state=TM_STATE_nextDollarRight_state;
                return 0;
            }
            return 1;
        case TM_STATE_uop_decode_10111:
            if (ucode_tape[ucode_tape_head]=='0') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101110;
                return 0;
            }
            if (ucode_tape[ucode_tape_head]=='1') {
                ucode_tape_head++;
                curr_state=TM_STATE_uop_decode_101111;
                return 0;
            }
            return 1;
    }
}